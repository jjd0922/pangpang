<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.newper.mapper.CompanyMapper">

    <select id="selectCompany" resultType="Map">
        SELECT
            *
        FROM
            COMPANY
                LEFT JOIN COMPANY_EMPLOYEE ON COM_CE_IDX = CE_IDX
        ORDER BY
            COM_IDX DESC
    </select>

    <select id="countCompany" resultType="Integer">
        SELECT
            COUNT(*)
        FROM
            COMPANY
                LEFT JOIN COMPANY_EMPLOYEE ON COM_CE_IDX = CE_IDX
    </select>

    <select id="selectCompanyContract" resultType="Map">
        SELECT
            *
        FROM
            COMPANY
                LEFT JOIN COMPANY_CONTRACT ON COM_IDX = CC_COM_IDX
    </select>

    <select id="countCompanyContract" resultType="Integer">
        SELECT
            COUNT(*)
        FROM
            COMPANY
                LEFT JOIN COMPANY_CONTRACT ON COM_IDX = CC_COM_IDX
    </select>
</mapper>