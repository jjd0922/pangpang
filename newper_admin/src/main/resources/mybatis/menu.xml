<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.newper.mapper.MenuMapper">

    <select id="selectSubMenuUrlByAuth" resultType="String">
        SELECT
            SM_URL
        FROM
            SUB_MENU,MENU
        WHERE
            SM_MENU_IDX=MENU_IDX
            AND JSON_CONTAINS(SM_AUTH,#{authIdx})
            AND SM_URL !=''
        ORDER BY MENU_ORDER, SM_ORDER
        LIMIT 1
    </select>
</mapper>