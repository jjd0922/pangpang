<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.newper.mapper.SpecMapper">
    <select id="selectSpecListValueList" resultType="String">
        SELECT DISTINCT(SPECL_VALUE) FROM SPEC_LIST
        WHERE SPECL_NAME=#{speclName}
        ORDER BY SPECL_NAME
    </select>
    <select id="selectSpecListMap" resultType="com.newper.entity.SpecList">
        SELECT SPECL_IDX, SPECL_VALUE FROM SPEC_LIST
        WHERE SPECL_NAME=#{speclName}
    </select>

    <insert id="insertSpecItem">
        INSERT INTO spec_item (
            SPECI_ORDER,
            SPECI_SPEC_IDX,
            SPECI_SPECL_IDX
        ) VALUES (
            #{SPECI_ORDER},
            #{SPEC_IDX},
            #{SPECL_IDX}
        )
    </insert>

    <insert id="insertSpecItemAll">
        INSERT INTO
            SPEC_ITEM
                (SPECI_ORDER, SPECI_SPEC_IDX, SPECI_SPECL_IDX)
        VALUES
            <foreach collection="speclIdxs" separator="," item="item" index="i">
                (#{i}, #{specIdx}, #{item})
            </foreach>
    </insert>
    <select id="selectSpecDataTable" resultType="Map">
        SELECT
        CATE_SPEC_LIST,
        PP_SPEC_IDX,
        POR_IN_SPEC_IDX,
        PP_SPEC_IDX2
       /* PX_BOM_IDX*/
        FROM
        PO_PRODUCT
        LEFT JOIN PRODUCT ON P_IDX = PP_P_IDX
        LEFT JOIN PO ON PP_IDX = PP_PO_IDX
        LEFT JOIN CATEGORY ON CATE_IDX = P_CATE_IDX
        LEFT JOIN PO_RECEIVED ON PP_IDX = POR_PP_IDX
        <if test="length!=-1">
            limit #{start},#{length}
        </if>
    </select>

    <select id="countSpecDataTable" resultType="Integer">
        SELECT
        COUNT(*)
        FROM
        PO_PRODUCT
        LEFT JOIN PRODUCT ON P_IDX = PP_P_IDX
        LEFT JOIN PO ON PP_IDX = PP_PO_IDX
        LEFT JOIN PO_RECEIVED ON PP_IDX = POR_PP_IDX
    </select>



</mapper>