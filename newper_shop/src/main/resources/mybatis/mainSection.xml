<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.newper.mapper.MainSectionMapper">
    <select id="selectMainSectionSp" parameterType="Integer" resultType="map">
        SELECT
            *
        FROM
            MAIN_SECTION
           ,MAIN_SECTION_SP
           ,SHOP_PRODUCT
           ,SHOP
        WHERE
            MS_SHOP_IDX = SHOP_IDX
          AND MSSP_MS_IDX = MS_IDX
          AND MSSP_SP_IDX = SP_IDX
          AND SHOP_IDX = #{shopIdx}
    </select>
</mapper>