package com.newper.repository;

import com.newper.entity.FloatingBar;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FloatingBarRepo extends JpaRepository<FloatingBar, Long> {
}
