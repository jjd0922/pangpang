<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.newper.mapper.ShopMapper">
    <sql id="includeShopDatatable">
        <if test="shopIdx != null and shopIdx !=''">
            AND SHOP_IDX = #{shopIdx}
        </if>
    </sql>
    <select id="selectShopDatatable" resultType="map">
        SELECT
            SHOP.*
            ,CONCAT(CREATED_DATE,' ',CREATED_TIME) CREATED_DATE_TIME
        FROM
            SHOP
        ORDER BY SHOP_IDX DESC
        <where>
            <include refid="includeShopDatatable"></include>
        </where>
        <if test="length!=-1">
            limit #{start},#{length}
        </if>
    </select>
    <select id="countShopDatatable" resultType="Integer">
        SELECT
            COUNT(*)
        FROM
            SHOP
        <where>
            <include refid="includeShopDatatable"></include>
        </where>
    </select>

    <select id="selectShopDesignJson" resultType="map">
        SELECT
            if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.FONT_SIZE'),'"',''),'') FONT_SIZE
             ,if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.SPACES'),'"',''),'') SPACES
             ,if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.BTN'),'"',''),'') BTN
             ,if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.EDGE'),'"',''),'') EDGE
             ,if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.BLACK'),'"',''),'') BLACK
             ,if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.WHITE'),'"',''),'') WHITE
             ,if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.POINT'),'"',''),'') POINT
             ,if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.POINT_FONT'),'"',''),'') POINT_FONT
             ,if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.MENU'),'"',''),'') MENU
             ,if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.MENU_FONT'),'"',''),'') MENU_FONT
             ,if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.TITLE'),'"',''),'') TITLE
             ,if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.BACK'),'"',''),'') BACK
             ,if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.LINE'),'"',''),'') LINE
             ,if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.THUMBNAIL'),'"',''),'') THUMBNAIL
             ,if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.FOOTER'),'"',''),'') FOOTER
             ,if(SHOP_DESIGN!='',replace(JSON_EXTRACT(SHOP_DESIGN,'$.SEARCH_BRAND'),'"',''),'') SEARCH_BRAND
        FROM
            SHOP
        WHERE SHOP_IDX = #{shopIdx}
    </select>
</mapper>
